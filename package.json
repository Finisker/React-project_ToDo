{
  "name": "react-express-localjson",
  "scripts": {
    "start": "node server.js",
    "build": "cd client && npm install && npm run build",
    "refresh": "npm run build && npm install && npm run start"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}